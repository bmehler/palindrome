language: php

php:
  - 5.3  

before_install:
  - composer selfupdate
  - composer install
  
script:
  - phpunit --bootstrap tests/autoload.php tests/ 
  - pyrus install pear/PHP_CodeSniffer
  - phpenv rehash
  - phpcs --standard=Zend ./
 